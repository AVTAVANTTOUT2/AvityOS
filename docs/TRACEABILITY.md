# Definition-of-done traceability

Evidence date: 2026-07-24. Status: ✅ implemented and locally reproduced ·
🟡 implemented/partial with the exact remaining proof or limitation stated.

| Requirement | Status | Reproducible evidence / limitation |
| --- | --- | --- |
| Complete project onboarding | ✅ | Public create/configuration/PATCH API; normalized objectives and budgets; server-side realpath, Git worktree, local branch and configured GitHub remote validation; Web create/edit/read-only configuration; equivalent CLI create/update/show options. Dedicated contract, control-plane, Web and CLI tests cover valid/no-repo creation, invalid paths, branch/remote validation, idempotent clarification retries, safe queued-plan supersession, in-flight revision conflicts, explicit empty-objective rejection, budget enforcement, homonymous isolation and real UI payload transmission. Local full gates passed; PR #30 passed [CI Linux #20](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29651872643) and [CI macOS #56](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29651872670). |
| Real web state, auth and browser behavior | 🟡 | Typed REST/SSE DataProvider, HttpOnly login, explicit offline/demo separation. `App.tsx` is split into `app/screens/*` and `app/components/*`; the stable control-plane project ID flows from cards and the ⌘K palette into a per-project detail, whose missions, agents, plan and PRs are filtered by ID (including homonymous-project coverage); terminal sessions are modeled per terminal with their own logs; legal mission cancellation, terminal cancellation and intervention choices call real endpoints. Complete onboarding create/edit/read-only state is wired to the public API; Playwright captures the actual multi-field browser payload. Project detail exposes a real `ClarificationPanel` and enables pause/resume only against the durable control-plane pause API (no optimistic simulation); Playwright `chantier3.spec.ts` covers grouped answer → pause → resume. Remaining: the browser onboarding/clarification E2E uses an intercepted API boundary while repository validation and pause fencing are proven in the real control-plane suite; settings sections beyond GitHub/Providers are placeholders. |
| Native macOS client | 🟡 | Swift build/test pass; Keychain, auth, deep links, notifications, Dock badge, settings/menu bar. The main window embeds the Figma Mission Control React UI via `WKWebView` (`avity-app://` static + `/v1` Keychain proxy, ADR-0021) as the application's only frontend: the simplified SwiftUI views are deleted and no flag restores them. XCUITests run against that shipped shell — embedded WebView present, native connection status, retired sidebar identifiers unreachable, toolbar and `avity://settings` opening the native Settings scene — while the React screens stay covered by vitest/Playwright. Swift tests cover bundle resolution, path-escape refusal and the proxy contract (encoded-body restoration since WebKit withholds `fetch` bodies from scheme handlers, dropped origin headers, Keychain bearer precedence, unbounded stream timeouts). Checkpoints 6.1–6.3 align contracts and ship CryptoKit host/device modes. Checkpoint 6.4 adds the shared Xcode project, universal Release `.app`, ZIP/checksum and recoverable install. Checkpoint 6.5 renews host/device certificates at D-30 in the encrypted channel. Checkpoint 6.6 defines the signed Ed25519 update channel with Gatekeeper validation and rollback. Remaining: operator Apple Developer ID/notarization, the first real HTTPS update feed, and an automated integration test of the embedded proxy against a live control plane (the write path is proven by unit tests and manual runs). ADR-0011/0012/0013/0021. |
| `avity` CLI | ✅ | 9 integration tests against an in-process authenticated control plane, including complete repository onboarding, idempotent update and atomic project pause/resume. |
| Durable/recoverable control plane | ✅ | SQLite migrations/event log; restart reconciliation scenario; idempotent clean-tree commits and unique PR row per mission. Vendor calls may be retried after a crash and are not exactly-once without vendor support. |
| Multiple isolated projects | ✅ | Scenario 3 proves concurrent project/event/usage isolation; generated missions are ordered within each project. |
| Per-project durable brain | ✅ | Decisions/results/risks with provenance persist and are injected into author and reviewer prompts; dedicated prompt test. |
| Central AI brain (chantier 2) | ✅ | Durable async pipeline objective → bounded secret-free repo snapshot → structured analysis → architecture → validated plan/DAG → role delegation, entirely through `ProviderAdapter` with strict Zod output, bounded repair, provider fallback, planning budget enforcement and explicit provenance. Ambiguous/infeasible analysis blocks before delegation; every mission uses the exact server-detected checks; snapshot/check discovery rejects escaping symlinks; provider artifacts are redacted before persistence. Evidence-based replanning is bounded, atomically idempotent and withdraws stale approvals while preserving history; inactive-plan approval races are refused; restart recovery reconciles orphan brain runs exactly once. Tests et surfaces : 15 tests pipeline, 10 plan-validation, 3 snapshot, CLI (`avity brain show`) et Web (`BrainPanel`). PR #32 : [CI macOS](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29660850182) / [CI Linux](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29660850199). Preuve live finale : DeepSeek `deepseek-v4-pro` a réussi l'analyse `brr_7b8cb02462334206a124`, l'architecture `brr_0aaf4feaf09540e7935b` et le plan réparé `brr_99252ffe79284efcbea9`, provenance `live`, matérialisant le plan actif `pln_2e874749b7e64dddae95` ensuite livré ; voir [`LIVE-E2E-EVIDENCE-2026-07-23.md`](./LIVE-E2E-EVIDENCE-2026-07-23.md). |
| Structured clarifications + atomic pause/resume (chantier 3) | ✅ | Contracts: `packages/contracts/src/clarification.ts` (`CLARIFICATION_SCHEMA_VERSION=1`, closed answer types, proposal/answer schemas) + pause/resume API types and error codes (`clarification_obsolete`, `clarification_incomplete`, `clarification_already_answered`, `project_paused`, `project_not_paused`). Persistence: SQLite migrations v6 (`project_pauses`, clarification metadata, mission `paused_from_state`) and v7 (`resume_pending`). Brain: clarification step via `ProviderAdapter` with deterministic policy gate, bounded rounds, answer injection, obsolete-on-objective-revision and exact-once resume. Engine: `pauseProject`/`resumeProject` cancel runs, revoke leases and fence late results; pause survives restart. **Concurrency audit (PR #35):** lease revocation is strictly project-scoped (P-ISO); critical Store transactions compare durable status and the workflow's captured `pause_generation`, so work from before a pause remains fenced after a fast resume across brain planning, run/terminal creation, usage, checkpoints, mission transitions, PR publication and the `validate`/`review`/`integrate`/check workflows (P-FENCE, `run.fenced`); clarification resume uses a claimed `resume_pending` outbox, atomically materializes all question decisions with per-question idempotency keys, recovers orphaned claims and drains them on restart or explicit project resume (P-RESUME). Surfaces: REST (`GET/POST clarifications`, `GET/POST pause`, `POST resume`) + SSE events `project.paused`/`project.resumed`/`clarification.obsolete`/`run.fenced`; CLI `avity clarification *` and `avity project pause|resume`; Web `ClarificationPanel` + enabled pause/resume. Tests: contract clarification suite + control-plane `chantier3.test.ts` (16) + `chantier3-hardening.test.ts` (21: shared-worker isolation, worker-endpoint/stale-generation/brain-run fencing, post-check and integration-cleanup races, crash-recovered/idempotent/explicit resume, late-reviewer fencing, path/secret/multi-choice validation, pause-transition matrix, v5→v7 migration) + Playwright `apps/web/e2e/chantier3.spec.ts`. Local reproduction (2026-07-19): `pnpm -r build` green; control-plane 107/107 outside the host sandbox; targeted chantier 3 hardening 21/21; `git diff --check` clean. |
| Provider runtime + fallback | 🟡 | Codex/Claude/Cursor CLI, OpenAI Responses, Anthropic, DeepSeek, trusted generic et fake enregistrés ; capacités honnêtes, routage par rôle et tests cross-provider. La campagne réelle prouve Codex et DeepSeek comme auteurs/reviewers distincts. La campagne du 24 juillet ajoute l'événement live certifiant `ev_1b3ba1cc91114d26bd90` : analyse DeepSeek `brr_87d42b3130cd4d9c8074` en échec contrôlé, action `switch_provider`, reprise immédiate et succès Codex `brr_8a9ce6bfc74e49bbb5cd`. Restent une mission Claude, une mission Cursor et la recertification DeepSeek après rotation de sa clé actuellement refusée HTTP 401 ; voir [`LIVE-E2E-EVIDENCE-2026-07-23.md`](./LIVE-E2E-EVIDENCE-2026-07-23.md) et [`LIVE-E2E-EVIDENCE-2026-07-24.md`](./LIVE-E2E-EVIDENCE-2026-07-24.md). |
| Live bounded correction and exhausted-review recovery | 🟡 | La campagne du 24 juillet a conservé un vrai défaut, les sorties de tests code 1 et trois corrections épuisées, puis une seconde mission rejetée par le reviewer à `3/3`. La PR #74 autorise désormais `review_required → failed`; la reprise live a produit `ev_3c605a4fb564440fa51e`, l'intervention `apr_c8da488535ee4950a8fb` et un control plane toujours disponible. La correction fonctionnelle finale n'est pas encore approuvée, donc le scénario reste partiel et n'est pas présenté comme réussi. |
| Worker execution architecture | ✅ | Capability/capacity matching, heartbeat, expiry, lease-token fencing, revocation, HTTPS policy, OS sandbox and cancellation. La PR #75 dérive aussi le statut affiché de la même fenêtre de heartbeat : validation live avec 1 worker courant `online` et 10 anciens `offline`, sans réécriture durable. |
| Remote bridge cryptographic foundation (chantier 5.1) | ✅ | Contrats stricts versionnés dans `@avityos/contracts` et primitives pures dans `@avityos/remote-bridge` : racine compte Ed25519, clés appareil Ed25519/X25519, certificats signés, appairage AES-256-GCM par secret 256 bits hors bande, enveloppes E2E à clé éphémère X25519/HKDF-SHA-256, signature de l'émetteur et garde de séquence anti-rejeu. Les tests négatifs couvrent secret erroné, expiration, session consommée, falsification, mauvais destinataire, rejeu et absence de clés privées/plaintext dans les structures relayables. ADR-0006 documente les métadonnées encore visibles. |
| Ciphertext relay + outbound connector (chantier 5.2) | ✅ | `@avityos/remote-relay` accepte uniquement les enveloppes strictes, borne/expire les inboxes, déduplique, long-poll et ack par curseur sans importer le control plane ni les primitives de déchiffrement. `RemoteRelayHttpClient` impose HTTPS hors loopback et garde le bearer hors URL. `RemoteOutboundConnector` poll uniquement en sortie, vérifie/déchiffre localement, appelle le handler local, chiffre la réponse et reprend un ack ambigu sans rejouer le handler. Le test E2E réseau prouve aller-retour, isolation de routage, absence de clair, auth, rejet plaintext, conflit d'ID, capacité et expiration. ADR-0007. |
| Durable remote authorization, revocation and audit (chantier 5.3) | ✅ | Le token global est désormais administrateur seulement ; `RemoteRelayAdminHttpClient` enrôle/rotate/révoque et chaque data-plane exige le bearer de l'appareil lié à l'émetteur/destinataire. `SqliteRelayStore` persiste en transactions appareils/token-hashes, ciphertexts, déduplication et curseurs monotones à travers redémarrage et expiration des messages, fichier `0600`. `RemoteBridgeStateStore` persiste uniquement confiance publique, session d'appairage secret-hash consommée atomiquement, révocation, état complet du connecteur et audit métadonnées chaîné/idempotent. La fabrique durable rend persistance + audit obligatoires. Tests : redémarrages réels, reprise d'ack sans handler/réponse dupliqué, révocation, altération d'audit détectée, aucun token/secret/clé privée/plaintext dans les DB. ADR-0008 ; side effects externes restent honnêtement at-least-once si le processus tombe au milieu du handler. |
| Native remote host mode (chantier 6.2) | ✅ | `RemoteHostManager` macOS conserve racine/identité et tokens admin/appareil dans Keychain via stdin, jamais argv ; SQLite ne reçoit que confiance publique, hash de session, révocation, reprise et audit. L'API locale bearer-authenticated/no-store gère statut, configuration, offre, acceptation et révocation. Le bootstrap AES-GCM cache certificat, URL relais et bearer aléatoire du nouvel appareil. Le connecteur sortant réinjecte en mémoire uniquement health/projets/missions/approvals/runs/terminaux et `approval.resolve`; une mutation hors allowlist échoue avant dispatch et est auditée. Tests : contrat/crypto bootstrap, corruption Keychain fail-closed, API complète, appairage/enrôlement/révocation et vrai aller-retour enveloppe distante chiffrée avec refus négatif. ADR-0009. |
| Native remote device mode (chantier 6.3) | ✅ | Le client Swift utilise CryptoKit avec les mêmes DER Ed25519/X25519, HKDF, AES-GCM, JSON canonique et contenus versionnés que Node. Identité privée, certificats, bearer, séquences, curseur et ack ambigu résident dans Keychain ; le bearer reste dans Authorization et HTTPS est obligatoire hors loopback. La séquence est persistée avant publication, puis séquence entrante + ack pending avant acquittement. Le vecteur certifiant généré par Node prouve certificats, bootstrap imbriqué et enveloppe host→device ; un faux relais chiffré prouve publish/poll/ack, ack déjà accepté après crash et rafraîchissement des cinq surfaces natives sans `/v1/remote-host`. ADR-0010. |
| Worktree → checks → commit → review | ✅ | Fixture repository E2E creates a real branch/worktree, forces a defect, executes checks, corrects, commits, independently reviews and cleans up. |
| Push + GitHub draft PR | ✅ | Injection-safe push/`gh pr` implementation and idempotent DB record. La campagne live a publié trois branches et ouvert automatiquement les PR draft [fixture #1](https://github.com/AVTAVANTTOUT2/avity-live-e2e-fixture/pull/1), [#2](https://github.com/AVTAVANTTOUT2/avity-live-e2e-fixture/pull/2) et [#3](https://github.com/AVTAVANTTOUT2/avity-live-e2e-fixture/pull/3), toutes encore ouvertes et non fusionnées. |
| Checkpoints and independent review | ✅ | Required commands pass only on exit evidence; separate reviewer run with diff/brain/evidence; rejection loops and approval tested. |
| Usage, quotas and budgets | ✅ | Transactional usage/budget accounting and budget escalation. Provider-reported cost remains zero unless pricing is configured. |
| Execution security boundary | ✅ | 7 control-plane security tests, 12 worker tests, environment test, Git-hook test and sandbox read/write tests cover the audited boundary. |
| Encrypted operator credential vault (chantier 7.1) | ✅ | `@avityos/credential-vault` chiffre un registre fermé par AES-256-GCM avec nonce frais et AAD version/key-id ; enveloppe bornée `0600`, mutations lockées/fsync/rename, refus mauvais key/tamper/symlink/mode/nom arbitraire/multiligne. Clé maître stdin-only dans Keychain macOS ou fichier owner-only explicitement extérieur au dépôt et à l'état opérateur sur Linux. `avity vault init/status/list/set/remove/migrate` ne sort jamais les valeurs ; migration préserve la précédence existante, vérifie le coffre avant suppression du clair et purge le bearer hérité de `cli.json`. Le lancement injecte uniquement le scope control-plane ou worker, filtre les credentials ambiants et le chemin de clé du Web/autres services ; le client CLI et `doctor` utilisent la même vue effective sans fallback `cli.json` en clair dès que le coffre existe. 9 tests package + 8 tests CLI couvrent crypto, absence de clair, locks, permissions, migration, rotation login et isolation. ADR-0014. |
| Vault recovery + certified backup/restore (chantier 7.2) | ✅ | Escrow portable strict `0600` hors dépôt/état : scrypt (`N=32768,r=8,p=1`) + AES-256-GCM avec paramètres/key-id authentifiés, passphrase stdin-only et jamais de clé brute/secret en sortie. Les key stores Keychain/fichier remplacent par compare-and-swap + readback ; la rotation stage le recovery suivant, re-chiffre atomiquement le coffre, rollback le key store avant commit et vérifie les trois vues. `avity backup create` capture le WAL en ligne via `VACUUM INTO`, puis certifie intégrité SQLite, foreign keys, migrations contiguës, chaîne d'audit recalculée, hashes et coffre avec l'escrow séparé. `verify` détecte l'altération ; `restore` exige le bundle ID exact, refuse toute destination existante, copie en flux borné et recertifie avant publication. Les DB persistantes refusent symlink/owner étranger et imposent chemin absolu, répertoire `0700`, fichiers/WAL `0600`. Preuves : 11 tests package, 66 CLI dont rotation/rollback/perte de clé et vrai backup WAL/restauration/altération, 243 control-plane dont 3 permissions/chemins DB. ADR-0015. |
| Native TLS + certificate-bound worker mTLS (chantier 7.3) | ✅ | `@avityos/transport-security` impose TLS 1.3, chemins PEM absolus, clés `0600` sous répertoire `0700`, refus des symlinks/owners étrangers et du plaintext control-plane hors loopback. CLI/worker utilisent une CA privée dans un transport dédié borné, sans affaiblir Node globalement. La migration v9 conserve les workers legacy non liés ; tout nouvel enrôlement mTLS persiste uniquement le fingerprint SHA-256, puis exige certificat exact + bearer sur lease/heartbeat/output/exit. Le test réseau réel génère CA/serveur/worker/rogue, refuse l'absence de certificat, exécute un vrai terminal, refuse le bearer volé avec un autre certificat pourtant signé par la même CA, préserve l'accès admin sans certificat, ajoute `Secure` au cookie HTTPS et ne divulgue pas le fingerprint. ADR-0016. |
| Transactional external credential activation (chantier 7.4) | ✅ | `avity vault credential-rotate NAME --stdin` limite la mutation aux huit credentials externes enregistrés, exige une valeur existante et un control-plane actif, puis applique un compare-and-swap chiffré, redémarre uniquement le control-plane et exécute 20 probes authentifiés bornés (health +, pour un credential fournisseur, provider enregistré sans `auth_missing`). Un échec restaure l'ancienne valeur par compare-and-swap et réactive le service ; une rotation concurrente n'est jamais écrasée et une ambiguïté de rollback reste fail-closed. Les bearers `AVITY_*` restent exclus de ce flux par redémarrage ; `AVITY_API_TOKEN` utilise désormais le protocole dédié du checkpoint 7.5. Preuves : primitive CAS package, activation réussie, rollback, double panne, conflit concurrent et interdiction des secrets en argv. ADR-0017. |
| Durable two-phase administrator bearer rotation (chantier 7.5) | ✅ | `avity vault credential-rotate AVITY_API_TOKEN --stdin` prépare en transaction un hash pending tout en gardant le hash current valide, applique le coffre par CAS, prouve deux routes protégées avec le nouveau bearer puis le promeut sans redémarrage. La migration v10 ne persiste aucun token en clair ; commit/abort sont idempotents, un redémarrage accepte current ou pending, le pont distant interne change immédiatement de bearer et une réponse de commit ambiguë conserve la nouvelle valeur pour reprise sûre. Un échec de preuve pré-commit restaure le coffre, annule pending et recertifie l'ancien client. Preuves : ancien/nouveau simultanés pendant prepare, bootstrap crash-safe, mismatch fail-closed, commit nouveau-only, révocation ancien, abort, absence de clair DB/résultat, rollback et reprise CLI. ADR-0018. |
| Certificate-bound two-phase worker bearer rotation (chantier 7.6) | ✅ | `avity vault worker-token-rotate` ne reçoit aucun secret : migration v11, token serveur aléatoire 192 bits, hash pending, worker `draining` et refus si terminal actif. Le coffre worker est modifié par CAS puis seul le worker redémarre ; un heartbeat frais avec pending — et l'exact certificat d'enrôlement si mTLS — est obligatoire avant commit, qui révoque l'ancien hash et repasse online. Rollback pré-commit, abort/commit idempotents, reprise pending, conflit concurrent et absence de clair DB/résultat sont testés. Le test réseau réel refuse aussi pending + certificat rogue signé par la même CA sans marquer la preuve. ADR-0019. |
| Two-phase worker mTLS certificate rotation (chantier 7.7) | ✅ | `avity tls worker-certificate-rotate --certificate … --private-key …` valide chemins/modes et compatibilité X.509/clé, puis la migration v12 conserve uniquement un fingerprint pending. Worker idle/draining, overlap current/pending, bearer inchangé et heartbeat frais autorisé par la CA sont obligatoires ; commit/abort idempotents, rollback des chemins protégés, reprise ambiguë et exclusion mutuelle avec la rotation bearer sont couverts. Le test TLS réel refuse commit sans preuve et certificat tiers signé par la même CA, accepte le candidat, puis rejette l'ancien après promotion. Aucun PEM/clé privée n'entre en SQLite, audit ou résultat. ADR-0020. |
| Supply-chain gates | ✅ | Local: zero known vulnerabilities after removing the unused vulnerable React Router RSC dependency and pinning patched `tar` 7.5.21; license policy passes 447 installed packages; Gitleaks passes on the staged patch. CI includes blocking audit/license/Gitleaks and SPDX SBOM. |
| Fake/demo honesty | ✅ | Fake is a deterministic engineering fixture; production backend failure shows `Hors ligne`; fixtures require `VITE_AVITY_DEMO=1`. |
| Full local TS verification | ✅ | `pnpm verify`: all 14 executable workspace builds/typechecks and 608 tests pass. |
| Browser + Swift verification | ✅ | Current checkout: Playwright 4/4 passed against its hermetic control plane; Swift strict-concurrency/warnings-as-errors and Thread Sanitizer pass 21 XCTest; native XCUITest passes 2/2 against the real app; the universal Release bundle, codesign, URL scheme and archive checksum are reproduced locally. macOS update CI also sandbox-extracts that archive, installs a distinct candidate, restores the exact original version and retains both rollback recovery copies. |
| Green GitHub Actions from corrected checkout | ✅ | PR #35 merged head: [CI Linux](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29678390360) and [CI macOS](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29678390381) pass build, 217 TypeScript tests, typecheck, 3 Playwright scenarios, Swift build/test, audit, licence policy, Gitleaks, SPDX SBOM and artifact upload. |
| No autonomous merge | ✅ | The AvityOS engine retains approved pull requests as drafts and contains no ready or merge operation; repository integration remains an explicit operator action. |
| E2E preflight contract + readiness cache + provider sandbox hardening (post-#38/#39 audit) | ✅ | Defensive hardening only (no product chantier closed). **Contract:** `E2EPreflightReport.superRefine` pins `realProviderCount`, `realWorkspaceEditorCount` (real editors only) and unique `providers[].name`, plus negative tests for scenario-count/keys/ready/blocked/readiness/non-negative counters. **Cache:** rejected GitHub readiness promises are evicted via `void value.catch` with identity guard (newer entry never clobbered); retry runs a fresh detection. **Providers:** `ProviderErrorCategory.sandbox_unavailable` (non-retryable escalate); command adapter emits it instead of `unknown`; hermetic unit tests inject `SandboxLauncher`/`ProcessSpawner`; OS isolation remains integration `skipIf(!SANDBOX_AVAILABLE)`. **Control-plane checks:** missing sandbox surfaces `sandbox_unavailable: …` instead of a fake check exit. **Validation locale (2026-07-21):** after installing Bubblewrap 0.9.0 (same family as CI Linux): `pnpm -r build`, `pnpm -r typecheck`, `pnpm -r test` (contracts 19, orchestration 17, providers 43+3 skipped, control-plane 181, policy 56+3 skipped, …), Playwright 3/3, `pnpm audit --audit-level high`, `pnpm licenses:check` (444), `git diff --check`; touched suites ×10 green. **Non exécutable ici:** `swift test` (pas de Swift sur cet hôte Linux) — preuve CI macOS. **Sans Bubblewrap:** hermetic providers verts; suites d’intégration sandbox skippées avec raison; checks control-plane fixture E2E échouent fail-closed (`sandbox_unavailable`) — même comportement sur `origin/main`. **CI:** head `2220c5b` [CI macOS verify](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29867387668) + [CI Linux verify-linux](https://github.com/AVTAVANTTOUT2/AvityOS/actions/runs/29867387643) SUCCESS (build, tests, typecheck, Playwright, Swift build/test on macOS, audit, licences, Gitleaks, SBOM). PR #41. |

## Mandatory scenarios

| # | Scenario | Evidence |
| --- | --- | --- |
| 1 | Clear objective → completion | control-plane scenario 1 |
| 2 | Ambiguous objective → grouped answer → resume | scenario 2 + `chantier3.test.ts` structured clarification suite |
| 3 | Concurrent projects remain isolated | scenario 3 |
| 4 | Rate-limit fallback, including cross-provider | scenario 4 + cross-provider test |
| 5 | Failed validation/review → bounded correction | scenario 5 + fixture defect/review rejection tests |
| 6 | Restart reconciliation | scenario 6; orphan failed once, mission resumes |
| 7 | Forbidden command/security boundary | control-plane security suite + worker scenario 7 |
| 8 | Cancellation cleans process tree/worktree | scenario 8 + runner process-group test |
